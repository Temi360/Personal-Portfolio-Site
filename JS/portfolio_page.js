// document.addEventListener("DOMContentLoaded", () => {
//   const originalSvg = document.getElementById("folderAndPin");
//   const svgContainer = document.getElementById("folders");
//   const numberOfCopies = 3; // Number of duplicates you want to create

//   for (let i = 0; i < numberOfCopies; i++) {
//     const clonedSvg = originalSvg.cloneNode(true); // Clone the SVG node with its children
//     svgContainer.appendChild(clonedSvg); // Append the cloned SVG to the container
//   }
// });
